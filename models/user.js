class User {
  constructor(id, imageUrl, firstName, lastName, phone) {
    this.id = id;
    this.imageUrl = imageUrl;
    this.firstName = firstName;
    this.lastName = lastName;
    this.phone = phone;
  }
}

export default User;
